@model IEnumerable<Programas.Models.ProgramaCircuito>

@{
    ViewData["Title"] = "Index";
}

<h2>Index</h2>

@*<p>
    <a asp-action="Create">Create New</a>
</p>*@
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Circuito)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Descripcion)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.PlanJefesHogar)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Pne)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Circuito)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Descripcion)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.PlanJefesHogar)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Pne)
            </td>
            @*<td>
                <a asp-action="Edit" asp-route-id="@item.Circuito">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.Circuito">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.Circuito">Delete</a>
            </td>*@
        </tr>
}
    </tbody>
</table>
